# LLMProtocols Overview

`LLMProtocols` defines service, backend, storage, lifecycle, tool, safety, and observability contracts.
It also owns backend-neutral lifecycle hooks such as runtime chat-session reset, while implementations remain in adapters.
