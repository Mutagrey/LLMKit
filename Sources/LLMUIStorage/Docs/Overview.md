# LLMUIStorage Overview

`LLMUIStorage` provides backend-neutral reusable SwiftUI storage presentation primitives.

`StorageUsageBarView` renders a one-dimensional stacked Swift Charts storage bar from host-provided byte segments.
