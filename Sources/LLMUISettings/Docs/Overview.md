# LLMUISettings Overview

`LLMUISettings` renders backend-neutral SwiftUI settings for LLM runtime configuration.

The screen groups model/routing, context, output, local memory, MLX, GGUF, safety, storage, and reset controls while using
only shared settings values and host-provided presentation context.
