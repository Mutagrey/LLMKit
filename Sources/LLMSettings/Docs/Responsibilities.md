# LLMSettings Responsibilities

Owns settings value types, recommended presets, constraints, normalization, effective token budget calculation, and
UserDefaults JSON persistence helpers. It does not import SwiftUI, backend SDKs, lifecycle services, storage services, or
orchestration.
