# LLMModelLifecycle Responsibilities

Owns lifecycle state transitions, model catalog data, declared artifact downloads, and installed record persistence. It does not execute prompts, own session history, import backend runtimes, or render progress UI.
