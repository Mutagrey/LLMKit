# LLMUIStorage Dependency Rules

May import `SwiftUI` and Apple UI frameworks such as `Charts`.

Must not import backend modules, lifecycle services, storage services, networking, orchestration, or package domain targets.
