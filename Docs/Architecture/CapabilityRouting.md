# Capability Routing

Routing is capability-driven. Requests describe required capabilities, execution mode, latency preference, quality tier, and optional preferred model. The orchestrator selects candidates from backend-neutral descriptors and then uses backend contracts for execution.
