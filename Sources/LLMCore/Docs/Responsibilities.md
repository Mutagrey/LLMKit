# LLMCore Responsibilities

Owns identifiers, descriptors, requests, results, events, usage metrics, tool DTOs, session DTOs, lifecycle state, policy values, and domain errors. It does not own protocols, routing, persistence, backend SDKs, or UI state.
