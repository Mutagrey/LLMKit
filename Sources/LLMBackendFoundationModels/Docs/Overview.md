# LLMBackendFoundationModels Overview

`LLMBackendFoundationModels` adapts Apple Foundation Models to generic backend contracts.

The current adapter exposes a runtime availability gate and model handle loading without importing Apple-specific SDK types into shared modules.
