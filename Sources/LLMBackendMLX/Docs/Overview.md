# LLMBackendMLX Overview

`LLMBackendMLX` is the future MLX-backed local model adapter.

The current adapter gates availability on supported model families and explicit host-provided runtime availability. It does not add an MLX dependency yet.
