# LLMBackendFoundationModels Public API

Public API is limited to generic backend conformers and availability helpers.

`FoundationModelsRuntimeAvailability` lets host code report whether the platform runtime is available before the backend advertises models as executable.
