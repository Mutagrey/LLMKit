# LLMExampleUI TODO

- Add a small sample app wrapper if the repository adopts an Xcode example app.
- Add configured Core ML or MLX downloadable descriptors once real download manifests are available.
- Add preview fixtures for unavailable Apple Intelligence states.
- Persist example settings when the package grows a dedicated demo app target.
