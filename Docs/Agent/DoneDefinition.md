# Done Definition

A phase is done when code compiles, tests cover introduced public invariants, docs match module responsibilities, dependencies remain valid, and no unapproved external dependencies were added.
