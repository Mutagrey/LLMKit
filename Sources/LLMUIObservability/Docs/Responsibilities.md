# LLMUIObservability Responsibilities

Owns runtime metrics presentation only. It does not collect metrics, choose routes, execute models, import backend targets,
or display prompt text, generated text, chat content, or tool payloads.
Memory indicators render host-supplied byte counts; collection remains outside this UI target.
