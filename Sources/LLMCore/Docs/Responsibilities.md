# LLMCore Responsibilities

Owns identifiers, descriptors, model source metadata, requests, results, events, usage metrics, privacy-safe runtime metric value types, tool DTOs, session DTOs, lifecycle state, cache policy values, routing policy values, and domain errors. It does not own protocols, routing, persistence, backend SDKs, download execution, cache storage, or UI state.
