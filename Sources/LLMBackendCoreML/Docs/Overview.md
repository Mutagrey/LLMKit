# LLMBackendCoreML Overview

`LLMBackendCoreML` adapts local Core ML model execution to generic backend contracts.

The current adapter exposes compatibility-based availability and model handle loading. Tokenization and execution remain future work.
